BEGIN TRANSACTION;
create table emp(emp_id integer primary key,name varchar(30),dept varchar(20));
insert into emp values(1,'anoy','automation');
insert into emp values(2,'an','automation');
insert into emp values(3,'bjoy','QA');
insert into emp values(4,'mayi','development');
insert into emp values(5,'anoy','development');
select * from emp;
create table empsalary(emp_id references emp, salary number(10,4));
insert into empsalary values(1,45000);
insert into empsalary values(1,50000);
insert into empsalary values(5,75000);
insert into empsalary values(2,30000);
insert into empsalary values(1,35000);
select * from empsalary;
select e.name,es.salary from emp e inner join empsalary es on e.emp_id=es.emp_id;
select e.name,es.salary from emp e left join empsalary es on e.emp_id=es.emp_id;
select e.name,es.salary from emp e right outer join empsalary es on e.emp_id=es.emp_id;
select e.name,es.salary from emp e join empsalary es on e.emp_id=es.emp_id;
select e.name, es.salary from emp e, empsalary es where es.salary > 40000 and e.emp_id=es.emp_id ;
select es.emp_id, es.salary from empsalary es, emp e where e.emp_id=2 and es.emp_id=2;
select count(*),avg(salary) from empsalary group by emp_id;
select avg(salary) from empsalary group by emp_id having count(*)>2;
select e.name,e.dept,es.salary from emp e, empsalary es where es.salary between 20000 and 50000 and e.emp_id=es.emp_id;
select name from emp where name like 's%';
select emp_id,name,dept from emp order by emp_id DESC;
select * from emp natural join empsalary;
select e.name,e.dept from emp e left join empsalary es on e.emp_id=es.emp_id union all select es.salary from empsalary es right join emp e on e.emp_id=es.emp_id;
select name from emp where name like '_n';
select name,emp_id,dept from emp where emp_id in ( select emp_id from empsalary where salary > 70000);